<Window x:Class="Plugin.Todolist.View.PopupOuvrirTodolistView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="395" d:DesignWidth="553" SizeToContent="WidthAndHeight">
    <Grid >
        <Label Content="Choisissez votre fichier" Height="30" HorizontalAlignment="Center" Name="label1" VerticalAlignment="Top" Width="300" />
        <DataGrid Width="310"  AutoGenerateColumns="False" ItemsSource="{Binding ListeCouranteTodolist}" Margin="0,37,0,53" SelectedItem="{Binding ProjetAOuvrir, UpdateSourceTrigger=PropertyChanged}" >
            <DataGrid.Columns>
                <DataGridTextColumn Width="300" Header="Nom du projet" Binding="{Binding NomDuProjet}" />
            </DataGrid.Columns>
        </DataGrid>
        <Button  Content="Ouvrir" Height="35" HorizontalAlignment="Left" Margin="163,309,0,0" Name="button1" VerticalAlignment="Top" Width="192" Command ="{Binding OuvrirProjetSelectionneCommand}" Click="button1_Click" />
    </Grid>
</Window>

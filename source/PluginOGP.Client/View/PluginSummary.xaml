<UserControl x:Class="PluginOGP.Client.View.PluginSummary"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="24" d:DesignWidth="300" Margin="50,25,50,25">
    <Border BorderBrush="SteelBlue" BorderThickness="2" CornerRadius="5">
        <Border.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="LightBlue" Offset="0.0"/>
                <GradientStop Color="White" Offset="0.6"/>
            </LinearGradientBrush>
        </Border.Background>
        <Border.Effect>
            <DropShadowEffect Opacity="0.5" Color="Black" ShadowDepth="3"/>
        </Border.Effect>
        <Expander Name="expander1" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
            <Expander.Header>
                <Grid HorizontalAlignment="Right" Width="{Binding Path=ActualWidth, ElementName=expander1}">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="300*"/>
                        <ColumnDefinition Width="80*"/>
                        <ColumnDefinition Width="20*"/>
                        <ColumnDefinition Width="20*"/>
                    </Grid.ColumnDefinitions>
                    <Label Content="Plugin name" Grid.Column="0" Margin="30,0,0,0" VerticalAlignment="Center"></Label>
                    <ProgressBar Grid.Column="1" HorizontalAlignment="Stretch" Height="Auto"  VerticalAlignment="Stretch" Width="Auto" Margin="12,4,12,4"/>
                    <Button Name="DownloadButton" Grid.Column="2" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Margin="4">
                        <Image Source="{StaticResource Telecharger}" Stretch="None" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Button>
                    <Button Name="UninstallButton" Grid.Column="3" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Margin="4">
                        <Image Source="{StaticResource Desinstaller}" Stretch="None" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Button>
                </Grid>
            </Expander.Header>
            
            <TextBox HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Text="Description here" Height="30"/>
        </Expander>                
     </Border>
</UserControl>
